# Local development helpers (safe defaults for UI work)
# Allows any logged-in user to access /admin routes while building the console.
# Remove or set to false for stricter role checks.
VITE_ALLOW_ADMIN_PREVIEW=true
